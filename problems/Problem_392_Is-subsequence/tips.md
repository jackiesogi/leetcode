## Is Subsequence

#### Approach
- Two pointers: One for s and one for t, both starting at 0. And at the end if s pointer is at the end,
,which means that all the char inside s can be found in t, return True else False.

